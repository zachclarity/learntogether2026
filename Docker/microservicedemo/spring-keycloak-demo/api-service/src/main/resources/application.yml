server:
  port: 8099

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://keycloak:8080/realms/springrealm

management:
  endpoints:
    web:
      exposure:
        include: health,info
